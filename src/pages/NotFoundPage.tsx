import React from "react";
import styled from "styled-components";

const NotFoundPage: React.FC = () => {
  return <StyledNotText>Not Page</StyledNotText>;
};
export default NotFoundPage;

const StyledNotText = styled.h2`
  text-align: center;
`;
